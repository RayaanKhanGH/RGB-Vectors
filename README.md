# RGB-Vectors

RGB vectors are whole new technology, it uses pixel manipulation and gets the rgb value of each pixel in an image, saves it to an array and outputs it.
This program is still not optimized for large images like 1920x1080. you can choose a pixel, it will be a tuple in the array, tuple[0] is the rgb combined value, tuple[1]
is the pixels index.

 
